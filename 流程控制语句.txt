流程控制语句：
if（条件表达式）{
      条件为真执行语句
}else{
      条件为假执行语句
}

else if:
if（条件一）{
      条件一满足时执行的代码
}else if（条件二）{
    条件二满足时执行的代码
}else if（条件三）{
    条件三满足时执行的代码
}else{
    所有条件都不满足时执行的代码
}


if嵌套（条件一）{
	if（条件1.1）
}else{
不满足条件一时执行的代码
}

开关:
switch（变量）{
    case0:

    case1:

    case2:
}
